# NGINX Production Configuration for Hostinger VPS / Ubuntu
server {
    listen 80;
    server_name api.newsx.ai admin.newsx.ai;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.newsx.ai;

    ssl_certificate /etc/letsencrypt/live/api.newsx.ai/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.newsx.ai/privkey.pem;

    # Gzip Compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 443 ssl http2;
    server_name admin.newsx.ai;

    ssl_certificate /etc/letsencrypt/live/admin.newsx.ai/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin.newsx.ai/privkey.pem;

    root /var/www/newsx/admin/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
