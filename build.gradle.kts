// Root-level build file
// This is kept minimal to allow the Flutter tool to manage the platform build
buildscript {
    repositories {
        google()
        mavenCentral()
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
